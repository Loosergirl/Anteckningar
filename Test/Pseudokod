START
  var redLine = true;
  var greenLine = true;
  var buss153 = true;
  var pendeln = true;

  if (redLine == false)
    console.log("Du stannar hemma");
  else
    console.log("Du tar buss 153");
    console.log("Du går från fridhemsplan")
  else if (redLine == true && pendeln == true)

START
  function calcylator(x, y, arithmetic)
    